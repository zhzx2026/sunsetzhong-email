{
  "name": "sunsetzhong-email",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "node scripts/bump-version.js && node scripts/sync-version.js && wrangler deploy",
    "version:sync": "node scripts/sync-version.js",
    "version:bump": "node scripts/bump-version.js && node scripts/sync-version.js"
  },
  "dependencies": {
    "hono": "^4.7.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250424.0",
    "wrangler": "^4.13.2"
  }
}
